{
  "name": "demo-app",
  "version": "1.0.0",
  "private": true,
  "description": "Demo application that consumes synced components and utils",
  "scripts": {
    "build": "tsc --noEmit && echo 'Build successful!'",
    "typecheck": "tsc --noEmit",
    "lint": "echo 'lint passed'",
    "dev": "echo 'Starting dev server...'",
    "test": "echo 'Tests passed!'"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "@types/react": "^18.2.0",
    "@types/react-dom": "^18.2.0"
  }
}
